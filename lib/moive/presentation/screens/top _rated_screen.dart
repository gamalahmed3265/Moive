import 'package:flutter/material.dart';

class TopRatedScrren extends StatelessWidget {
  const TopRatedScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(), body: const Center(child: Text("Top Rated")));
  }
}
